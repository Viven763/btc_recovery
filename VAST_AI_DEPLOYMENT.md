# Развертывание на Vast.ai

Полное руководство по запуску Ethereum BIP39 Recovery на облачных GPU Vast.ai.

## 📋 Предварительная подготовка

### 1. Подготовка оркестратора

Оркестратор должен быть доступен из интернета. Варианты:

**Вариант A: Локальный сервер с ngrok**
```bash
# Запустить оркестратор
cd ../bip39-solver-server
node index.js &

# Туннель через ngrok
ngrok http 3000
# Запомните URL: https://abcd1234.ngrok.io
```

**Вариант B: Облачный VPS**
```bash
# На вашем VPS (DigitalOcean, AWS, etc.)
cd bip39-solver-server
node index.js

# Настройте firewall для порта 3000
# Используйте публичный IP: http://your-vps-ip:3000
```

### 2. Загрузка базы данных на облачное хранилище

База данных `eth20240925` (4.3 GB) слишком большая для Docker образа.
Загрузите её на доступное хранилище:

**Вариант A: Google Drive**
```bash
# Загрузите eth20240925 на Google Drive
# Получите публичную ссылку для скачивания
```

**Вариант B: AWS S3 / DigitalOcean Spaces**
```bash
aws s3 cp eth20240925 s3://your-bucket/eth20240925 --acl public-read
```

**Вариант C: Ваш собственный сервер**
```bash
# На сервере с базой данных
python3 -m http.server 8000
# База будет доступна по http://your-server:8000/eth20240925
```

## 🚀 Создание Docker образа

### Опция 1: Локальная сборка и пуш в Docker Hub

```bash
# 1. Войти в Docker Hub
docker login

# 2. Собрать образ
cd eth_recovery
docker build -t yourusername/eth-recovery:latest .

# 3. Push в Docker Hub
docker push yourusername/eth-recovery:latest
```

### Опция 2: Использовать GitHub Actions для автоматической сборки

Создайте `.github/workflows/docker.yml`:

```yaml
name: Build and Push Docker Image

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: ./eth_recovery
          push: true
          tags: yourusername/eth-recovery:latest
```

## 🌐 Регистрация на Vast.ai

1. Создайте аккаунт: https://vast.ai/
2. Пополните баланс (минимум $10-20 для теста)
3. Настройте SSH ключ в профиле

## 🔍 Поиск подходящих GPU

### Рекомендуемые конфигурации:

**Для теста (1-2 часа):**
- 1x RTX 3060 Ti или лучше
- 12+ GB VRAM
- $0.10-0.20/час

**Для production (10-20 дней):**
- RTX 3090 / RTX 4090
- 24+ GB VRAM
- 10-100 инстансов
- $0.25-0.40/час за GPU

### Команды поиска через CLI:

```bash
# Установить Vast CLI
pip install vastai

# Войти
vastai login

# Поиск доступных GPU
vastai search offers 'gpu_name=RTX_4090 num_gpus=1 total_flops>50 reliability>0.95' --order 'dph+'

# Пример вывода:
# ID    GPU           VRAM  DPH
# 1234  RTX 4090      24GB  $0.35
# 5678  RTX 4090      24GB  $0.38
```

## 🎬 Запуск инстанса

### Через Web интерфейс:

1. Перейдите в "Search" -> "GPU Instances"
2. Фильтры:
   - GPU: RTX 3090 или RTX 4090
   - Min Reliability: 95%
   - CUDA Version: 12.x
3. Нажмите "Rent" на выбранном инстансе
4. Настройте:
   - **Docker Image**: `yourusername/eth-recovery:latest`
   - **On-start script**:
     ```bash
     #!/bin/bash
     # Скачать базу данных
     mkdir -p /app/data
     wget -O /app/data/eth20240925 http://your-server/eth20240925

     # Установить переменные окружения
     export WORK_SERVER_URL=https://your-ngrok-url.ngrok.io
     export WORK_SERVER_SECRET=your-secret-key

     # Запустить worker
     cd /app
     ./target/release/eth_recovery
     ```
   - **Disk Space**: 10 GB (для базы данных)

### Через CLI:

```bash
# Создать инстанс
vastai create instance <INSTANCE_ID> \
  --image yourusername/eth-recovery:latest \
  --env WORK_SERVER_URL=https://your-ngrok.io \
  --env WORK_SERVER_SECRET=your-secret \
  --onstart-cmd 'wget -O /app/data/eth20240925 http://your-server/eth20240925 && /app/target/release/eth_recovery' \
  --disk 10

# Проверить статус
vastai show instances

# Посмотреть логи
vastai ssh-url <INSTANCE_ID>
ssh root@<SSH_URL>
docker logs <CONTAINER_ID>
```

## 📊 Мониторинг

### Логи на Vast.ai:

```bash
# SSH в инстанс
vastai ssh-url <INSTANCE_ID>
ssh root@<SSH_URL>

# Посмотреть Docker контейнеры
docker ps

# Логи worker
docker logs -f <CONTAINER_ID>

# Проверить GPU утилизацию
nvidia-smi -l 1
```

### Логи оркестратора:

```bash
# На сервере с оркестратором
cd bip39-solver-server
tail -f logs/*.log

# Проверить прогресс в SQLite
sqlite3 work.db "SELECT COUNT(*) FROM completed_work"
```

## 🎯 Масштабирование до 100 GPU

### Стратегия запуска:

1. **Тест на 1 GPU** (1 час):
   - Проверить что всё работает
   - Измерить скорость (комбинаций/сек)
   - Оценить время завершения

2. **Масштаб до 10 GPU** (1 день):
   - Проверить стабильность
   - Мониторить CPU load на оркестраторе
   - Настроить автоматический restart

3. **Полный масштаб 100 GPU** (10-20 дней):
   - Использовать Spot instances для экономии
   - Настроить уведомления (Telegram/Email)
   - Регулярные backups прогресса

### Автоматизация через скрипт:

```bash
#!/bin/bash
# launch_100_workers.sh

for i in {1..100}; do
  echo "Launching worker $i..."

  vastai search offers 'gpu_name=RTX_4090 num_gpus=1' \
    --order 'dph+' -n 1 --format json \
    | jq -r '.[0].id' \
    | xargs -I {} vastai create instance {} \
      --image yourusername/eth-recovery:latest \
      --env WORK_SERVER_URL=$ORCHESTRATOR_URL \
      --env WORK_SERVER_SECRET=$SECRET \
      --onstart-cmd 'wget -O /app/data/eth20240925 $DB_URL && /app/target/release/eth_recovery'

  sleep 10  # Задержка между запусками
done

echo "Launched 100 workers!"
```

## 💰 Оценка стоимости

### RTX 4090 (лучшая производительность):
- Скорость: ~200k комбинаций/сек
- Время на 8.6B комбинаций: ~12 часов на 1 GPU
- **100 GPU:** ~7.2 минут теоретически, реально ~30 минут
- Цена: $0.35-0.40/час × 100 GPU × 0.5 часа = **$17-20**

### RTX 3090 (лучшая цена/производительность):
- Скорость: ~150k комбинаций/сек
- Время: ~16 часов на 1 GPU
- **100 GPU:** ~10 минут реально
- Цена: $0.25/час × 100 GPU × 0.2 часа = **$5**

## ⚠️ Важные замечания

### Безопасность:
- Используйте сложный SECRET для оркестратора
- Не храните приватные ключи в Docker образе
- Немедленно выводите средства при находке

### Стабильность:
- Некоторые инстансы могут отключиться
- Оркестратор автоматически переназначит работу
- Регулярно проверяйте логи

### Оптимизация затрат:
- Используйте Interruptible instances (дешевле на 50%)
- Запускайте в регионах с низкой ценой
- Останавливайте неиспользуемые инстансы

## 🎉 После находки решения

1. Worker автоматически отправит мнемонику на оркестратор
2. Проверьте логи оркестратора: `cat found_mnemonics.log`
3. **НЕМЕДЛЕННО** импортируйте мнемонику в безопасный кошелёк
4. Переместите средства на новый адрес с высоким gas price
5. Остановите все инстансы на Vast.ai

```bash
# Остановить все инстансы
vastai show instances --format json | jq -r '.[].id' | xargs -I {} vastai destroy instance {}
```

## 📚 Дополнительные ресурсы

- Vast.ai документация: https://vast.ai/docs
- Vast.ai CLI: https://github.com/vast-ai/vast-python
- Мониторинг: Grafana + Prometheus (опционально)

---

**Удачи в восстановлении! 🚀💰**

# 🚀 Готовый скрипт для Vast.ai

Просто скопируйте и вставьте этот скрипт в поле "On-start script" на Vast.ai.

**ВАЖНО:** Замените только 2 строки:
1. `ORCH` - IP адрес вашего VPS
2. `SECRET` - SECRET KEY из оркестратора

---

## 📋 Скрипт для копирования

```bash
#!/bin/bash
# ИЗМЕНИТЕ ЭТИ 2 СТРОКИ:
ORCH="http://YOUR_VPS_IP:3000"
SECRET="YOUR_SECRET_KEY"

# Остальное не трогайте:
DB_URL="https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip"
REPO=""

set -e
apt-get update -qq && apt-get install -y -qq curl wget git build-essential pkg-config libssl-dev ocl-icd-opencl-dev clinfo unzip > /dev/null 2>&1
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
source $HOME/.cargo/env
mkdir -p /workspace && cd /workspace
if [ -n "$REPO" ]; then git clone "$REPO" eth_recovery; else echo "No repo - using mounted code"; fi
cd eth_recovery 2>/dev/null || { echo "eth_recovery folder not found!"; exit 1; }
if [ ! -f eth20240925 ]; then
    echo "Downloading database..."
    wget -q --show-progress "$DB_URL" -O eth20240925.zip
    echo "Extracting..."
    unzip -q eth20240925.zip
    rm eth20240925.zip
    echo "Database ready!"
fi
echo "Building project..."
cargo build --release
export WORK_SERVER_URL="$ORCH" WORK_SERVER_SECRET="$SECRET" DATABASE_PATH="/workspace/eth_recovery/eth20240925"
echo "Starting worker..."
./target/release/eth_recovery 2>&1 | tee worker.log
```

---

## 🔧 Конфигурация Vast.ai инстанса

При создании инстанса укажите:

### Обязательные настройки:
- **Image**: `nvidia/cuda:12.2.0-devel-ubuntu22.04`
- **Disk Space**: 15 GB (минимум)
- **On-start script**: Скопируйте скрипт выше

### Рекомендуемые GPU:
- RTX 4090 (лучшая производительность)
- RTX 3090 (дешевле, чуть медленнее)
- RTX 3080 Ti (бюджетный вариант)

### Фильтры поиска:
- Min Reliability: 95%
- CUDA Version: 12.x
- Sort by: $/hr (дешевле первыми)

---

## ⚠️ Важно перед запуском

### 1. Оркестратор должен быть запущен на VPS:

```bash
ssh root@YOUR_VPS_IP

# Проверьте что оркестратор работает:
curl http://localhost:3000/status

# Должен вернуть JSON с "status": "running"
```

### 2. Получите SECRET KEY:

```bash
# На VPS посмотрите логи оркестратора:
journalctl -u eth-recovery-orchestrator | grep SECRET

# Или если запускали вручную:
cat ~/bip39-solver-server/logs/orchestrator.log | grep SECRET
```

### 3. Узнайте IP адрес VPS:

```bash
# На VPS:
curl ifconfig.me

# Или:
hostname -I
```

---

## 📊 Процесс запуска worker (что происходит)

1. **~2 мин** - Установка зависимостей (Rust, OpenCL, build tools)
2. **~3 мин** - Скачивание базы данных (4.3 GB)
3. **~1 мин** - Распаковка архива
4. **~5-10 мин** - Компиляция проекта
5. **Старт** - Worker начинает работу

**Общее время до начала работы: 10-15 минут**

---

## 🔍 Мониторинг worker

### SSH подключение к инстансу:

```bash
# Получите SSH команду
vastai show instances

# Подключитесь (команда будет выглядеть примерно так):
ssh root@ssh5.vast.ai -p 12345
```

### Просмотр логов:

```bash
# Логи worker
tail -f /workspace/eth_recovery/worker.log

# Мониторинг GPU
watch nvidia-smi

# Проверка процесса
ps aux | grep eth_recovery
```

### Признаки успешной работы:

Вы должны видеть в логах:
```
✅ Компилирование OpenCL kernel...
✅ OpenCL kernel скомпилирован
🔥 Запуск GPU вычислений...
📊 Прогресс: X.XX% | Скорость: ~XXXk комбинаций/сек
```

---

## 🎉 При находке решения

Worker автоматически:
1. Отправит мнемонику на оркестратор
2. Продолжит работу (на случай если есть еще совпадения)

Решение сохранится на VPS в:
```
/opt/eth-recovery-orchestrator/logs/FOUND_SOLUTIONS.txt
```

### Что делать:

```bash
# 1. Проверьте файл на VPS
ssh root@YOUR_VPS_IP
cat /opt/eth-recovery-orchestrator/logs/FOUND_SOLUTIONS.txt

# 2. НЕМЕДЛЕННО импортируйте мнемонику в безопасный кошелек

# 3. Проверьте баланс

# 4. Переведите средства на новый адрес с ВЫСОКИМ GAS PRICE

# 5. Остановите все workers
vastai destroy instance <INSTANCE_ID>
```

---

## 🚀 Масштабирование

Хотите запустить несколько workers одновременно?

```bash
# Через веб-интерфейс:
# Просто создайте несколько инстансов с тем же скриптом

# Через CLI (10 workers):
for i in {1..10}; do
  vastai create instance <OFFER_ID> \
    --image nvidia/cuda:12.2.0-devel-ubuntu22.04 \
    --disk 15 \
    --env ORCH=http://YOUR_VPS_IP:3000 \
    --env SECRET=YOUR_SECRET_KEY \
    --onstart-file vast_onstart_inline.sh
  sleep 5
done
```

### Производительность:

| Workers | Скорость | Время на 8.6B | Стоимость |
|---------|----------|---------------|-----------|
| 1x 4090 | ~200k/сек | ~12 часов | $4 |
| 10x 4090 | ~2M/сек | ~1 час | $3.50 |
| 100x 4090 | ~20M/сек | ~7 минут | $4-5 |

---

## 🆘 Troubleshooting

### Worker не может скачать БД:
```bash
# Проверьте URL вручную:
wget --spider https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip

# Должно показать: HTTP/1.1 200 OK
```

### Worker падает при компиляции:
```bash
# Увеличьте disk space до 20 GB
# Проверьте что Rust установился:
ssh root@VAST_IP -p PORT
rustc --version
cargo --version
```

### Worker не подключается к оркестратору:
```bash
# На VPS проверьте firewall:
ufw status
ufw allow 3000/tcp

# Проверьте что порт открыт:
netstat -tlnp | grep 3000

# С worker попробуйте подключиться:
curl http://YOUR_VPS_IP:3000/status
```

### OpenCL не находит GPU:
```bash
# Проверьте драйверы:
nvidia-smi

# Проверьте OpenCL:
clinfo

# Должно показать ваш GPU
```

---

## ✅ Готово!

Скопируйте скрипт, замените 2 переменные, и запускайте на Vast.ai!

**Удачи! 🚀💰**

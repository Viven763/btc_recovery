# 🚀 Deployment Files - Ethereum BIP39 Recovery

Все файлы для развертывания системы восстановления seed-фразы на Vast.ai с VPS оркестратором.

---

## 📁 Файлы в этой папке

### 🎯 Для быстрого старта:

1. **ГОТОВЫЙ_СКРИПТ.md** (7.0K) ⭐ **НАЧНИТЕ С ЭТОГО**
   - Готовый скрипт для копирования в Vast.ai
   - Нужно изменить только 2 строки (ORCH и SECRET)
   - Полная инструкция по запуску

2. **QUICK_START.md** (8.2K)
   - Быстрая инструкция (30 минут до запуска)
   - Пошаговые команды
   - Мониторинг и troubleshooting

3. **DEPLOYMENT_CHECKLIST.md** (11K)
   - Детальный пошаговый чеклист
   - Все варианты развертывания
   - Полная документация

---

### 🛠️ Скрипты для VPS (оркестратор):

1. **orchestrator_setup.sh** (9.8K)
   - Полная автоматическая установка оркестратора
   - Установка Node.js, создание systemd service
   - Генерация SECRET KEY

2. **create_orchestrator_service.sh** (1.9K)
   - Быстрое создание systemd service
   - Для случаев когда оркестратор уже установлен
   - Исправление проблем с существующим service

---

### 🎮 Скрипты для Vast.ai (workers):

1. **vast_onstart_inline.sh** (1.1K) ⭐ **ИСПОЛЬЗУЙТЕ ЭТОТ**
   - Компактный скрипт для вставки в веб-форму Vast.ai
   - Все переменные конфигурации в начале файла
   - Автоматическая установка, компиляция, запуск

2. **vast_worker_startup.sh** (5.9K)
   - Расширенная версия с подробными комментариями
   - Используется как референс или для модификации
   - Можно использовать через `--onstart-file` в CLI

---

## 🚀 Быстрый старт (3 шага)

### Шаг 1: Установите оркестратор на VPS

```bash
# На вашем VPS (Ubuntu 22.04)
wget https://raw.githubusercontent.com/YOUR_REPO/eth_recovery/main/deploy/orchestrator_setup.sh
chmod +x orchestrator_setup.sh
sudo bash orchestrator_setup.sh

# Сохраните SECRET KEY из вывода!
```

**Или создайте service вручную:**
```bash
# Если оркестратор уже установлен вручную
sudo bash create_orchestrator_service.sh
```

### Шаг 2: Откройте ГОТОВЫЙ_СКРИПТ.md

- Скопируйте скрипт из файла
- Замените `YOUR_VPS_IP` на IP вашего VPS
- Замените `YOUR_SECRET_KEY` на SECRET из шага 1

### Шаг 3: Запустите worker на Vast.ai

1. Зайдите на https://vast.ai/
2. Search → GPU Instances
3. Выберите RTX 4090 или RTX 3090
4. Configuration:
   - Image: `nvidia/cuda:12.2.0-devel-ubuntu22.04`
   - Disk: 15 GB
   - On-start script: Вставьте ваш скрипт
5. Нажмите "Rent"

**Готово!** Worker начнет работу через 10-15 минут.

---

## 📊 Что происходит автоматически

### На VPS (оркестратор):
✅ Установка Node.js
✅ Создание рабочей директории `/opt/eth-recovery-orchestrator`
✅ Установка npm зависимостей
✅ Инициализация SQLite базы с 512 заданиями
✅ Создание systemd service с автозапуском
✅ Генерация SECRET KEY
✅ Настройка firewall (порт 3000)
✅ Запуск API сервера

### На Vast.ai worker:
✅ Установка системных зависимостей (build tools, OpenCL)
✅ Установка Rust toolchain
✅ Скачивание базы данных (4.3 GB ZIP)
✅ Распаковка архива
✅ Клонирование кода (если указан REPO)
✅ Компиляция Rust проекта (5-10 минут)
✅ Подключение к оркестратору
✅ Старт GPU вычислений

---

## 🔧 Конфигурация

### Переменные окружения для worker:

```bash
ORCH="http://YOUR_VPS_IP:3000"              # URL оркестратора
SECRET="YOUR_SECRET_KEY"                     # SECRET KEY
DB_URL="https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip"  # URL базы данных
REPO=""                                      # GitHub репо (опционально)
```

### База данных:

**По умолчанию используется:**
```
https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip
```

Это архив с базой данных 357M Ethereum адресов (4.3 GB распакованный).

**Можете использовать свою копию:**
1. Разместите файл `eth20240925` на доступном HTTP сервере
2. Измените переменную `DB_URL` в скрипте

---

## 📈 Производительность и стоимость

| Конфигурация | Скорость | Время (8.6B комбинаций) | Стоимость |
|-------------|----------|-------------------------|-----------|
| 1x RTX 4090 | ~200k/сек | ~12 часов | $4 |
| 10x RTX 4090 | ~2M/сек | ~1 час | $3.50 |
| 100x RTX 4090 | ~20M/сек | ~7 минут | $4-5 |
| 1x RTX 3090 | ~150k/сек | ~16 часов | $3 |
| 10x RTX 3090 | ~1.5M/сек | ~1.5 часа | $2.50 |

**Рекомендация:** Начните с 1 GPU для теста, затем масштабируйте до 10-100.

---

## 🔍 Мониторинг

### Проверка оркестратора:

```bash
# Статус сервиса
systemctl status eth-recovery-orchestrator

# Логи
journalctl -u eth-recovery-orchestrator -f

# API статус
curl http://YOUR_VPS_IP:3000/status

# Прогресс в базе данных
sqlite3 /opt/eth-recovery-orchestrator/work.db "SELECT
    COUNT(*) as total,
    SUM(CASE WHEN completed_at IS NOT NULL THEN 1 ELSE 0 END) as completed,
    ROUND(100.0 * SUM(CASE WHEN completed_at IS NOT NULL THEN 1 ELSE 0 END) / COUNT(*), 2) as progress
FROM work_queue;"
```

### Проверка worker:

```bash
# Получить SSH доступ
vastai show instances

# Подключиться
ssh root@ssh5.vast.ai -p PORT

# Логи
tail -f /workspace/eth_recovery/worker.log

# GPU загрузка
nvidia-smi -l 1

# Процесс
ps aux | grep eth_recovery
```

---

## 🎉 При находке решения

### Автоматически происходит:
1. Worker находит совпадение
2. Отправляет мнемонику на оркестратор
3. Оркестратор сохраняет в файл и БД

### Проверка на VPS:

```bash
# Найденные решения
cat /opt/eth-recovery-orchestrator/logs/FOUND_SOLUTIONS.txt

# Или через БД
sqlite3 /opt/eth-recovery-orchestrator/work.db "SELECT * FROM found_solutions;"
```

### Немедленные действия:

1. **ИМПОРТИРУЙТЕ** мнемонику в безопасный кошелек
2. **ПРОВЕРЬТЕ** баланс
3. **ПЕРЕВЕДИТЕ** средства на новый адрес (высокий gas!)
4. **ОСТАНОВИТЕ** все workers: `vastai destroy instance <ID>`
5. **УДАЛИТЕ** логи с мнемоникой

---

## 🆘 Troubleshooting

### Оркестратор не запускается:

```bash
# Проверьте логи systemd
journalctl -u eth-recovery-orchestrator -n 50

# Проверьте что Node.js установлен
node --version

# Проверьте рабочую директорию
ls -la /opt/eth-recovery-orchestrator/

# Переустановите service
sudo bash deploy/create_orchestrator_service.sh
```

### Worker не может подключиться к оркестратору:

```bash
# На VPS откройте порт
ufw allow 3000/tcp

# Проверьте что оркестратор слушает на всех интерфейсах
netstat -tlnp | grep 3000

# С worker проверьте доступность
curl http://YOUR_VPS_IP:3000/status
```

### Worker падает при компиляции:

```bash
# Увеличьте disk space до 20 GB
# Проверьте что Rust установлен
rustc --version
cargo --version

# Проверьте логи компиляции
cat /workspace/eth_recovery/worker.log
```

### База данных не скачивается:

```bash
# Проверьте URL
wget --spider https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip

# Попробуйте скачать вручную
wget -O test.zip https://cryptoguide.tips/btcrecover-addressdbs/eth20240925.zip
unzip test.zip
ls -lh eth20240925
```

---

## 📚 Дополнительная документация

- **../README.md** - Техническая документация проекта
- **../VAST_AI_DEPLOYMENT.md** - Детальная инструкция по Vast.ai
- **../../FINAL_SUMMARY.md** - Общая сводка проекта

---

## ⚠️ Важные замечания

### Безопасность:
- ✅ Используйте сложный SECRET KEY (генерируется автоматически)
- ✅ Не загружайте приватные ключи на GitHub
- ✅ Немедленно выводите средства при находке
- ✅ Удалите все логи после завершения

### Стоимость:
- VPS оркестратор: ~$6/месяц
- Vast.ai GPU: $0.20-0.50/час за GPU
- Полный перебор на 10 GPU: ~$3-5

### Время:
- Установка оркестратора: 5 минут
- Первый worker startup: 10-15 минут
- Последующие workers: 10-15 минут
- Полный перебор (10 GPU): ~1 час

---

## ✅ Checklist перед запуском

- [ ] VPS с Ubuntu 22.04 арендован
- [ ] Оркестратор установлен и запущен
- [ ] SECRET KEY сохранен
- [ ] API `/status` отвечает корректно
- [ ] Порт 3000 открыт в firewall
- [ ] Баланс Vast.ai пополнен ($10-20)
- [ ] Скрипт подготовлен с правильными ORCH и SECRET
- [ ] План действий при находке решения готов

---

**Готово к запуску! 🚀💰**

Удачи в восстановлении seed-фразы!

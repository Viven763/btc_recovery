# Быстрый старт

## 1. Запуск оркестратора (bip39-solver-server)

```bash
cd ../bip39-solver-server
npm install
node index.js
```

Оркестратор запустится на `http://localhost:3000`

## 2. Запуск GPU Worker

```bash
cd eth_recovery
cargo run --release
```

## 3. Текущий статус

✅ **Готово:**
- ✅ Структура проекта создана
- ✅ Database loader с binary search работает
- ✅ OpenCL устройство обнаружено (Apple M4 Pro)
- ✅ База данных загружается (357M записей, 4.3GB)
- ✅ API клиент для оркестратора реализован
- ✅ Конфигурация и документация готовы

⚠️ **Осталось реализовать:**
1. Завершить eth_recovery_main.cl kernel
2. Добавить PBKDF2-HMAC-SHA512 (BIP39 seed generation)
3. Добавить BIP44 key derivation (m/44'/60'/0'/0/0)
4. Добавить Keccak256 (Ethereum address generation)
5. Интегрировать DB lookup в GPU kernel

## 4. Что уже есть

### Структура проекта
```
eth_recovery/
├── src/
│   ├── main.rs           ✅ GPU Worker с оркестратором
│   └── db_loader.rs      ✅ Binary search в БД
├── cl/
│   ├── eth_recovery_main.cl  ⚠️ Заготовка kernel
│   ├── keccak256.cl          ✅ Keccak готов
│   ├── sha2.cl              ✅ SHA2 готов
│   └── secp256k1_*.cl       ✅ ECDSA готов
├── config.json          ✅ 20 известных слов
├── eth20240925          ✅ 344M ETH адресов
└── README.md            ✅ Полная документация
```

### Рабочие компоненты

1. **Database Loader** (`db_loader.rs`):
   - Загружает БД в память (4.3 GB)
   - Сортирует записи по addr_suffix
   - Binary search O(log n)
   - Протестирован: 10/10 успешных поисков

2. **Main Worker** (`main.rs`):
   - Подключение к OpenCL
   - Загрузка БД
   - API клиент для оркестратора:
     - `GET /work` - получить задание
     - `POST /work` - отметить завершение
     - `POST /mnemonic` - отправить решение

3. **OpenCL Kernels** (`cl/`):
   - Все базовые компоненты скопированы из bip39-solver-gpu
   - Нужна интеграция в единый kernel

## 5. Следующие шаги

### Вариант 1: Завершить GPU kernel
Реализовать полный pipeline на GPU:
```
Mnemonic (4 варианта)
  → BIP39 Seed (PBKDF2)
  → BIP44 Derive
  → ETH Address (Keccak256)
  → DB Lookup
```

### Вариант 2: CPU прототип (для тестирования)
Сначала реализовать на CPU для проверки логики:
- Генерация мнемоник ✅
- BIP39 → Seed (tiny-keccak)
- BIP44 derivation (secp256k1)
- Keccak256 для адресов
- DB lookup ✅

Затем портировать на GPU.

### Вариант 3: Использовать готовый btcrecover
Адаптировать btcrecover для Ethereum вместо с нуля писать GPU код.

## 6. Оценки производительности

### На 1 GPU (RTX 4090):
- Полный pipeline: ~100-200k комбинаций/сек
- Время: ~2.8 года

### На 100 GPU:
- Время: **~10 дней**

### На 1000 GPU (Vast.ai):
- Время: **~1 день**
- Стоимость: $0.10/час × 1000 × 24 = **~$2,400**

## 7. Развёртывание

### Локально (тест)
```bash
# Терминал 1: Оркестратор
cd ../bip39-solver-server
node index.js

# Терминал 2: Worker
cd eth_recovery
cargo run --release
```

### На Vast.ai

1. Создать Docker образ:
```dockerfile
FROM nvidia/cuda:12.0-runtime-ubuntu22.04
RUN apt-get update && apt-get install -y ocl-icd-opencl-dev curl
COPY target/release/eth_recovery /app/
COPY eth20240925 /app/
COPY cl/ /app/cl/
WORKDIR /app
CMD ["./eth_recovery"]
```

2. Загрузить БД на S3/DO Spaces:
```bash
aws s3 cp eth20240925 s3://your-bucket/
```

3. Запустить на Vast.ai:
```bash
vastai create instance \
  --image your-docker-image \
  --env WORK_SERVER_URL=http://your-server:3000 \
  --env DB_URL=https://your-bucket.s3.amazonaws.com/eth20240925
```

## 8. Безопасность

⚠️ **КРИТИЧЕСКИ ВАЖНО:**

1. После нахождения мнемоники:
   - **НЕМЕДЛЕННО** переведите средства
   - Используйте high gas price (100+ gwei)
   - Отправьте на заранее подготовленный кошелёк

2. Защита данных:
   - Храните WORK_SERVER_SECRET в секрете
   - Не коммитьте БД в git
   - Шифруйте найденные мнемоники
   - Используйте HTTPS для production

3. Vault для мнемоник:
```bash
# Зашифровать найденную мнемонику
echo "mnemonic words" | gpg -e -r your@email.com > found.gpg
```

## 9. Мониторинг

### Проверить прогресс
```bash
# API оркестратора
curl http://localhost:3000/status

# Логи worker
tail -f worker.log
```

### Метрики
- Батчей обработано
- Скорость (комбинации/сек)
- ETA (осталось времени)

## 10. Помощь

### Проблемы с OpenCL
```bash
# Проверить устройства
clinfo

# Для Apple Metal
# Убедитесь что установлен Xcode
xcode-select --install
```

### Проблемы с БД
```bash
# Проверить размер
ls -lh eth20240925

# Должно быть ~4.3 GB
```

### Проблемы с оркестратором
```bash
# Проверить порт
lsof -i :3000

# Логи
cd ../bip39-solver-server
tail -f nohup.out
```

---

**Готов к запуску после завершения GPU kernel! 🚀**
